import CacheSelector from '../__cache-selector';

const { $list, $links } = {
  ...CacheSelector.storeInfo,
};

function showEndpoints() {

}



function init() {
  showEndpoints();
}

export default init;
